services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgresql://whale_alpha:whale_alpha@postgres:5432/whale_alpha
      REDIS_URL: redis://redis:6379
    ports:
      # Whale-wallet ingestion webhook (integrations/helius_webhook.py).
      # Only needed if you're pointing a real indexer at this local instance
      # (e.g. via ngrok) â€” safe to leave mapped for local dev either way.
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: whale_alpha
      POSTGRES_PASSWORD: whale_alpha
      POSTGRES_DB: whale_alpha
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U whale_alpha"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
