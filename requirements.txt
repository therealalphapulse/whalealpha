# --- v3 dependencies, unchanged ---
aiogram==3.11.0
python-dotenv==1.0.1
aiohttp==3.10.11
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
Pillow==10.4.0
solders==0.21.0
base58==2.1.1
cryptography==43.0.3

# --- v4 additions (Architecture Bible) ---
# Database migrations (§7) — replaces the hand-written migrate_*_schema()
# functions as the source of truth for schema changes.
alembic==1.13.2

# Externalized state: FSM storage, provider cache, distributed locks (§5).
# Optional at runtime — infra/locks.py and providers/cache.py both fall
# back to an in-memory implementation if this isn't installed or
# REDIS_URL isn't set, but it's required for any multi-instance deployment.
redis==5.0.8

# Metrics endpoint (§10) — infra/observability/metrics.py no-ops without
# it, but it's required to actually expose /metrics for Prometheus.
prometheus-client==0.20.0

# Error tracking (§10). Optional — only initialized if SENTRY_DSN is set.
sentry-sdk==2.13.0

# AWS KMS backend for infra/kms/kms_provider.py (§8). Only required when
# KMS_PROVIDER=aws_kms; not needed for the default env-var provider.
boto3==1.34.162
